



document.addEventListener("DOMContentLoaded", function getTotalTag(){

    let tagName = `form`;
    let totalTag = document.getElementsByTagName(tagName);

    console.log("total tag named " + tagName +` is: `+ totalTag.length);

});